# Estado de la Carpeta `.iteraspec`

El protocolo actual de IteraSpec requiere que cada ciclo use su propio workspace dentro de `.iteraspec/<feature_name>/`.

Workspace activo de este ciclo:
- `.iteraspec/iteraspec-gui-viewer/`
